package com.ui;

import java.util.Date;
import java.util.Scanner;

import javax.persistence.EntityManager;

import com.util.JPAUtil;

import cts.model.Student;

public class Example1ForCompositePK {

	public static void main(String[] args) {
		
		/*
		 * Student s=new Student(new StudentIdentity(1, 'A', 7), "uma", "mahesh", new
		 * Date(2020,01,01));
		 * 
		 * EntityManager em=JPAUtil.getEntityManagerFactory().createEntityManager();
		 * em.getTransaction().begin(); em.persist(s); em.getTransaction().commit();
		 * 
		 * System.out.println("emp saved..."); JPAUtil.shutdown();
		 */		
				

	}

}
